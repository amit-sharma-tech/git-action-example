name: 01 - Builing Blocks

on: workflow_dispatch

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Successfull step
              run: |
               echo "I will Success!"
               exit 0
            - name: Say goodbye
              run: echo "Goodbye!"
